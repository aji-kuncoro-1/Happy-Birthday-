import { defineConfig } from 'vite'

export default defineConfig({
  // Relative asset paths make the project work on any GitHub Pages
  // repository name without needing to edit this file again.
  base: './',
})
